name: Build and Deploy
permissions:
  contents: write

on:
  push:
    branches:
      - main
  workflow_dispatch:

env:
  URL: 'https://static.petals.team'

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v4
        with:
          persist-credentials: false

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version-file: '.nvmrc'

      - name: Install and Build 🔧
        run: |
          npm install
          npx tailwindcss -i ./css/app.css -o ./css/app.min.css

      - name: Deploy 🚀
        uses: JamesIves/github-pages-deploy-action@releases/v4
        with:
          branch: gh-pages
          folder: dist
